// From value.html Snippet 13
alge Shape {
    Rect( real, real ),
    Circle( real ),
}

let s = Shape.Rect( 10.0, 20.0 );

match s {
    case .Rect( w, h ) { print( "Rect:", w * h ); }
    case .Circle( r ) { print( "Circle:", r * r * 3.14 ); }
}

