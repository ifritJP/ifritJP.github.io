// From syntax.html Snippet 7
fn errorCase( flag: bool ) {
    let val: int;
    if flag {
        val = 10;
    }
    // else 節がないため、flag が false の場合に初期化されない
    // print( val ); // Error: variable 'val' may be uninitialized
}

