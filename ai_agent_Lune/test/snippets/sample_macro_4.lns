// From macro.html Snippet 4
macro _repeatPrint( count: int, msg: str ) {
    // expand-statement (実際に生成されるコード)
    let mut i = 0;
    while i < ,,count {
        print( ,,msg );
        i = i + 1;
    }
}
_repeatPrint( 3, "Hello" );

