// From tutorial_macro.html Snippet 1

// @lnsFront: ok
macro _d( val:__exp ) {
    // ,,,,val (カンマ4つ) は式を文字列化します
    // ,,val   (カンマ2つ) は式を展開（評価）します
    print( ,,,,val, " = ", ,,val );
}

let x = 100;
_d( x );
_d( x + 50 );

