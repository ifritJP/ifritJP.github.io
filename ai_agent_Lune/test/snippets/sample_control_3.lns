// From control.html Snippet 3
alge Result {
    Val( int ),
    Err( str ),
}
fn check( res: Result ) {
    match res {
        case .Val( val ) {
            print( "Success:", val );
        }
        case .Err( msg ) {
            print( "Error:", msg );
        }
    }
}

