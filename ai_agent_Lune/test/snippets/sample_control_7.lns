// From control.html Snippet 7
let list = [ 10, 20, 30 ];
foreach val, index in list {
    print( index, val ); // 1 10, 2 20, 3 30
}

let map = { "a": 1, "b": 2 };
foreach val, key in map {
    print( key, val );
}

