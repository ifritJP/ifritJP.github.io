// From class.html Snippet 10
class User extend (Mapping) {
    let name: str {pub};
    let age: int {pub};
}

// JSON (Map) から生成
let user = unwrap User._fromMap( { "name": "John", "age": 20 } );
// Map へ変換
let map = user._toMap();

