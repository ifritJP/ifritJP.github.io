// From value.html Snippet 10
fn funcReturnsNilable(): int! { return nil; }
let! x = funcReturnsNilable() {
    // x が nil の場合に実行される
    // 必ず x に値をセットするか、return で抜ける必要がある
    x = 0; 
} then {
    // x が非 nil の場合に実行 (省略可)
};

