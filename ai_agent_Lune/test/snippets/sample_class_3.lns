// From class.html Snippet 3
class Data {
    pri let mut val: int {pub, pub};
    pub fn __init( val: int ) { self.val = val; }
}
let mut d = new Data( 1 );
print( d.$val ); // getter 呼び出し ($記号)
d.set_val( 20 ); // setter 呼び出し (set_メンバ名)
print( d.$val );

