
macro _measure( target:__exp ) {
   {
      // Try to use it in expression context
      let _ = ,,target;
   }
}
fn voidFunc() {
    print("void");
}
fn intFunc(): int {
    return 1;
}

_measure( intFunc() );
// _measure( voidFunc() ); // This would fail if let _ expects value
